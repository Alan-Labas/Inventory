export type userResponse = {
    id: number,
    name: string,
    surname: string,
    username: string,
    email: string,
    password: string,
    createdAt: Date,
    isActive: boolean,
    role: string,
}